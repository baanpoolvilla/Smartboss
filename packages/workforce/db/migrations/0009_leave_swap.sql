-- สลับวันหยุด — พนักงานขอเปลี่ยนวันหยุดที่ลงไปแล้วเป็นอีกวันหนึ่ง
--
-- ต่างจากยกเลิก+ขอใหม่สองใบตรงที่ต้องรอ "อนุมัติ" เสมอ แม้ประเภทการลานั้น
-- จะเป็นชนิด auto_approve (เช่นวันหยุดประจำเดือน) — เพราะการสลับกระทบวันที่
-- คนอื่นวางแผนงานไว้แล้ว จึงให้หัวหน้าเห็นก่อนมีผลจริง
--
-- ระหว่างรออนุมัติ วันเดิมยังคงเป็นวันหยุดของพนักงานตามปกติ (ไม่แตะใบเดิม
-- จนกว่าจะอนุมัติ) เก็บ swap_from_date ไว้ที่ใบใหม่เพื่อให้ decideRequest
-- รู้ว่าต้องไปยกเลิกใบไหนพร้อมกันตอนอนุมัติ
ALTER TABLE workforce.leave_requests
  ADD COLUMN IF NOT EXISTS swap_from_date date;
