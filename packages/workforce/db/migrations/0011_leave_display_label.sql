-- ชื่อที่ขึ้นบนปฏิทินของใบลาแต่ละใบ ตั้งเองได้
--
-- เดิมปฏิทินประกอบชื่อขึ้นเองจาก "ชื่อคน - ชื่อประเภท" ซึ่งอ่านยากเวลาช่องแคบ
-- และไม่พอกับที่ทีมใช้กันจริง: บนปฏิทิน Teams ที่ใช้อยู่เดิม แต่ละคนตั้งชื่อ
-- วันหยุดของตัวเองเป็น "Bee-Off" · "Aui-V3/6" · "Parguy-Off-OT" ซึ่งมีข้อมูล
-- ที่ระบบไม่รู้ (กะที่สลับ, ควงต่อ OT) และเป็นภาษาที่ทีมอ่านแล้วเข้าใจทันที
--
-- ว่าง = ให้ปฏิทินประกอบชื่อให้เองเหมือนเดิม ไม่ใช่ขึ้นเป็นช่องว่าง
ALTER TABLE workforce.leave_requests
  ADD COLUMN IF NOT EXISTS display_label text NOT NULL DEFAULT '';
