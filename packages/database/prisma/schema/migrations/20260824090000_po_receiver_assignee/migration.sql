-- มอบหมาย "ผู้รับของ" ตอนกดดำเนินการซื้อ
--
-- เดิมมีแต่ po_assigned_to = คนไปซื้อ แล้วใครกดรับของก็ถูกบันทึกเป็น received_by
-- แต่คนซื้อกับคนรับของมักไม่ใช่คนเดียวกัน (สั่งของออนไลน์แล้วให้คนเฝ้าออฟฟิศเซ็นรับ)
-- ⇒ ไม่มีที่ให้ระบุล่วงหน้าว่าใครต้องไปรับ และคนนั้นก็กดปุ่ม "รับของ" ไม่ได้ด้วย
--    เพราะสิทธิ์เช็คแค่ po_assigned_to กับ created_by
--
-- NULL = ยังไม่ระบุผู้รับ (ใบเดิมทั้งหมด) ความหมายไม่เปลี่ยน ไม่ต้อง backfill

-- AlterTable
ALTER TABLE "maintenance"."purchase_orders" ADD COLUMN "receiver_assigned_to" TEXT;
