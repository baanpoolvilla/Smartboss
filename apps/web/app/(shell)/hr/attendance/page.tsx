import { redirect } from "next/navigation";

/**
 * "การลงเวลา" ย้ายไปเป็นหน้าแรกของโมดูลแล้ว (/hr)
 *
 * เดิมหน้าแรกเป็นการ์ดสรุปที่คำนวณแล้ว ซึ่งถูกตัดออกทั้งหมด — สิ่งที่คนเปิด
 * โมดูลบุคคลอยากรู้ก่อนคือ "วันนี้ใครมาแล้วบ้าง" ไม่ใช่ยอดรวมย้อนหลัง 30 วัน
 * เก็บเส้นทางเดิมไว้ redirect กันลิงก์/บุ๊กมาร์กที่คนบันทึกไว้แล้วพัง
 */
export default function AttendanceRedirect() {
  redirect("/hr");
}
