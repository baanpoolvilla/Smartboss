/**
 * ย้ายไปเป็นคอมโพเนนต์กลางที่ @/components/module/app-scaffold แล้ว
 * (โมดูล hr และ admin ใช้ตัวเดียวกัน) — ไฟล์นี้คง re-export ไว้ให้หน้าเดิมไม่ต้องแก้ import
 */
export {
  AppScaffold,
  AppBarLink,
  Fab,
} from "@/components/module/app-scaffold";
