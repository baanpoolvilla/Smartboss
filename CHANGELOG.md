# Changelog

**อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้งที่ pull code ใหม่มา** — เพื่อให้รู้ว่าใครแก้อะไรไป
ต้องทำอะไรต่อ (migration/seed/deploy) และมีอะไรที่ยังค้างอยู่บ้าง

## วิธีเพิ่มรายการ

แก้โค้ดเสร็จ (commit แล้ว) ให้เพิ่มบล็อกใหม่ไว้ **บนสุด** ของหัวข้อ "## บันทึก" ด้านล่าง
ตามฟอร์แมตนี้:

```
### YYYY-MM-DD HH:MM — <ชื่อคุณ>
**<feat/fix/docs>:** <สรุปสั้น ๆ ว่าทำอะไร>
- ทำอะไร: <รายละเอียดพอเข้าใจ ไม่ต้องยาว>
- ไฟล์/branch หลัก: <ไฟล์หรือโฟลเดอร์สำคัญ>
- ต้องทำหลัง pull: <migration/seed/deploy ที่ต้องรัน หรือเขียน "ไม่มี">
- ค้างอยู่ / ต้องระวัง: <ถ้ามี>
```

ใช้เวลาปัจจุบันจริง (เขตเวลาไทย) — จะเอามาจาก `git log -1 --date=format:'%Y-%m-%d %H:%M'` ของ
commit ที่เพิ่งทำก็ได้ ไม่ต้องเดา

---

## บันทึก

### 2026-08-12 16:32 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เรียงการ์ดงานอัตโนมัติ (เสร็จลงล่าง/เรียงตามความเร่งด่วน) + เลือก "หัวหน้าหลัก" ของงานกลุ่มได้
- ทำอะไร:
  1. การ์ดงานในทุกคอลัมน์ของบอร์ด Kanban และหน้ารายการงานแยกหัวข้อโปรเจคของแต่ละคน จะเรียงลำดับ
     อัตโนมัติแล้ว — งาน "เสร็จสิ้น" ลงไปอยู่ล่างสุดเสมอ ส่วนงานที่ยังไม่เสร็จเรียงจากกำหนดส่งใกล้สุด/
     เลยกำหนดมากสุดขึ้นก่อน
  2. งานที่มีผู้รับผิดชอบหลายคน (งานกลุ่ม) เลือกได้ว่าใครเป็น "หัวหน้าหลัก" — เลือกตอนสร้างงานได้เลย
     หรือกดดาว ⭐ ข้างชื่อทีหลังในหน้ารายละเอียดงานก็ได้ เป็นแค่ป้ายแสดงผล **ไม่มีผลกับสิทธิ์แก้ไข/
     มองเห็นงาน** (สิทธิ์ยังมาจากผู้มอบหมาย/หัวหน้าแผนกเหมือนเดิม) ถอดคนที่เป็นหัวหน้าออกจากผู้
     รับผิดชอบ ป้ายจะหลุดให้เองอัตโนมัติ
- ไฟล์หลัก: `lib/task-flags.ts` (`sortTasksForDisplay`), `components/kanban/kanban-board.tsx`,
  `components/kanban/person-topics-board.tsx`, `store/task-store.ts` (`setMainAssignee`),
  `components/kanban/new-task-dialog.tsx`, `components/kanban/task-detail-sheet.tsx`,
  `components/kanban/task-card.tsx`, `types/index.ts` (`Task.mainAssigneeId`)
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 16:17 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** แก้อัปโหลดไฟล์แนบ/รูปไม่ได้เลย (POST /api/report-task/uploads ขึ้น 500)
- ทำอะไร: ผู้ใช้ทดสอบจริงบนโปรดักชันแล้วกด "แนบไฟล์" ไม่มีอะไรเกิดขึ้นเลย เช็ค DevTools เจอ
  `POST /api/report-task/uploads 500` — สาเหตุคือโค้ดเรียก `crypto.randomUUID()` แบบพึ่ง global
  ของ Node ตรงๆ บนเซิร์ฟเวอร์ ซึ่งไม่การันตีว่ามีเสมอทุกเวอร์ชัน Node ต่างจาก `storage.ts` (ใช้งาน
  ได้จริงอยู่แล้ว) ที่ import `randomUUID` จาก `"node:crypto"` ตรงๆ — แก้ให้ทุกจุดที่เรียกบนเซิร์ฟเวอร์
  ใช้ import แบบเดียวกันหมด แก้ไปด้วยว่า `/api/report-task/tasks/sweep` (หักคะแนนงานเลยกำหนด
  อัตโนมัติ) ก็เรียกแบบเดียวกัน แต่ error ถูกกลืนเงียบๆ ไม่มี toast แจ้ง จึงพังมาเงียบๆ โดยไม่มีใครรู้
- เปลี่ยนปุ่ม "+ ตกลง" ในช่องเพิ่มเช็คลิสต์ (หน้ารายละเอียดงาน) เป็น "+ เพิ่ม" ให้ตรงกับปุ่มเดียวกัน
  ในหน้าสร้างงานใหม่
- ไฟล์หลัก: `app/api/report-task/uploads/route.ts`, `app/api/report-task/tasks/sweep/route.ts`,
  `lib/db/ics-link-repo.ts`, `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ถ้า deploy แล้วยังอัปโหลดไม่ได้ ให้เช็คว่า `sudo -u smartboss node --version`
  บนเครื่อง production ตรงกับที่ package.json กำหนดไว้ (`>=20`) จริงไหม — ถ้าต่ำกว่านี้อาจมีจุดพังแบบ
  เดียวกันซ่อนอยู่ที่อื่นอีก

### 2026-08-12 16:02 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat/fix:** สลับโหมดงานเดี่ยว/กลุ่มอัตโนมัติตามจำนวนผู้รับผิดชอบ + ล็อก "มอบหมายโดย" ห้ามเปลี่ยน
- ทำอะไร: เดิมกด "+ เพิ่มคน" ในผู้รับผิดชอบได้เสมอไม่ว่างานจะเป็นเดี่ยวหรือกลุ่ม แต่ `taskMode` ไม่เคย
  อัปเดตตาม ทำให้งานที่เพิ่มเป็น 2 คนแต่ยังนับเป็น "เดี่ยว" กดสถานะเป็น "เสร็จสิ้น" ตรงๆ ได้เลยโดยไม่รอ
  ให้ทุกคนติ๊กเช็คลิสต์ครบก่อน (ขัดกับพฤติกรรมงานกลุ่มจริง) — แก้โดยให้ `setAssignees` (จุดเดียวที่ทุก
  หน้าจอเรียกใช้ตอนเพิ่ม/ลดคน) สลับ `taskMode` ให้เองอัตโนมัติ: เหลือ 1 คน = งานเดี่ยว, 2 คนขึ้นไป =
  งานกลุ่ม ไม่ต้องมีเมนูแยกให้เลือกโหมด และล็อกช่อง "มอบหมายโดย" ในหน้ารายละเอียดงานให้อ่านอย่างเดียว
  เสมอ (เดิมผู้จัดการเปลี่ยนได้) เพราะเป็นข้อเท็จจริงว่าใครมอบหมายงานจริง ไม่ควรแก้ย้อนหลังได้ — มีผลกับ
  สิทธิ์แก้ไข/มองเห็นงานด้วย
- เพิ่มไอคอน "i" ข้างหัวข้อ "ไฟล์แนบ" ในหน้ารายละเอียดงาน (ทั้งไฟล์แนบของงานและไฟล์แนบในคอมเมนต์)
  กดดูได้ว่าบริษัทนี้ตั้งขนาด/จำนวนไฟล์สูงสุดไว้เท่าไหร่ (อ่านค่าจากหน้าตั้งค่า "ไฟล์แนบ" เดียวกัน)
- ไฟล์หลัก: `store/task-store.ts` (`setAssignees`), `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 15:32 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ลบฟีเจอร์ "เปิดงานใหม่" ให้แก้ไขกำหนดส่งดึงงานที่เสร็จกลับมาทำแทน
- ทำอะไร: เอาปุ่ม/ฟอร์ม "เปิดงานใหม่" กับป้าย "แก้ไขงาน — งานยังไม่เรียบร้อย (N ครั้ง)" ออกทั้งจาก
  การ์ด Kanban และแผงรายละเอียดงาน (สาเหตุที่ตัวนับมักขึ้น 0 ทั้งที่มีป้ายแสดง: `moveTask` ตั้ง
  `reopenedOnce` เองทุกครั้งที่ลากงานออกจาก "เสร็จสิ้น" คนละทางกับตัวนับจริงที่มาจาก revision
  ที่ติดแท็กตอนกดเปิดงานใหม่เท่านั้น — เลยไม่ตรงกัน) แทนที่ด้วย: ถ้าใช้ "แก้ไขกำหนดส่ง" กับงานที่
  สถานะ "เสร็จสิ้น" อยู่ ระบบจะดึงสถานะกลับเป็น "กำลังทำ" ให้อัตโนมัติในตัวเลย ไม่ต้องมีปุ่มแยก
- ไฟล์หลัก: `store/task-store.ts` (ลบ action `reopenTask`, แก้ `reviseDueDate`),
  `components/kanban/task-card.tsx`, `components/kanban/task-detail-sheet.tsx`,
  `lib/task-flags.ts` (ลบ `reopenCount` ที่ไม่มีที่ใช้แล้ว)
- ต้องทำหลัง pull: ไม่มี migration (field `reopenedOnce` เดิมใน type/schema ปล่อยไว้เฉยๆ
  เผื่องานเก่าที่มีค่านี้อยู่ ไม่กระทบอะไร)
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 (ช่วงบ่าย) — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** แนบไฟล์จริงในคอมเมนต์ + ตั้งค่าขนาด/จำนวนไฟล์แนบได้เองต่อบริษัท
- ทำอะไร: ช่องคอมเมนต์เอาปุ่มแท็ก @ ออก เปลี่ยนเป็นแนบไฟล์/รูปแทน (อัปโหลดจริง ไม่ใช่ metadata
  ปลอมเหมือนของเดิม) — ระหว่างทางเจอว่าปุ่ม "แนบไฟล์" ในหน้ารายละเอียดงานเป็นของปลอมมาตลอด
  (สร้างไฟล์ "1.2 MB PDF" หลอกๆ ไม่อัปโหลดจริง) แก้ให้เป็นของจริงพร้อมกัน — เพิ่มหน้าตั้งค่า
  "ไฟล์แนบ" ให้ปรับขนาดสูงสุดต่อไฟล์ (แยกรูป/เอกสาร/วิดีโอ) และจำนวนไฟล์สูงสุดต่องาน/ต่อ
  ความคิดเห็นได้เอง — ฝั่งเซิร์ฟเวอร์ (`/api/report-task/uploads`) บังคับตามค่านี้จริง ไม่ใช่แค่ UI
- ไฟล์หลัก: `apps/web/modules/report_task/store/attachment-settings-store.ts` (ใหม่),
  `lib/task-attachment-upload.ts` (ใหม่), `components/shared/attachment-settings-panel.tsx` (ใหม่),
  `components/kanban/task-detail-sheet.tsx`, `app/api/report-task/uploads/route.ts`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: **ยังไม่ผูกกับแพ็กเกจสมัครสมาชิก (planCode) อัตโนมัติ** — ตอนนี้เป็นค่าที่
  เจ้าของบริษัทตั้งเอง (ทุกบริษัทเริ่มด้วยค่าเริ่มต้นเดียวกัน: รูป/เอกสาร 8MB, วิดีโอ 25MB, งานละ 20
  ไฟล์, คอมเมนต์ละ 5 ไฟล์) ยังไม่มีจุดไหนในระบบอ่าน `planCode` ไปจำกัดอะไรอัตโนมัติ — รอตัวเลขจริง
  ต่อแพ็กเกจถึงจะผูกอัตโนมัติได้ ก็ยังไม่ได้แก้ไฟล์ที่ไม่ใช่รูปในตอนสร้างงาน/ประชุม (`new-task-dialog.tsx`)
  ให้อัปโหลดจริง — ยังเป็นข้อจำกัดเดิมที่ค้างมาก่อนรอบนี้

### 2026-08-12 15:30 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** "หัวข้อโปรเจค" — แท็กจัดกลุ่มงานใหม่ แทนระบบเทมเพลตเดิม (ยังไม่ deploy — รอทดสอบ)
- ทำอะไร: เพิ่มแท็ก "หัวข้อโปรเจค" ให้ติดกับงานได้ (ไม่บังคับ ไม่มีผลสิทธิ์อะไร) แทนที่ช่อง
  "เทมเพลต" เดิมในฟอร์มสร้างงาน — เลือกหัวข้อที่มีอยู่ หรือ "+ สร้างหัวข้อใหม่..." ได้เลย
  ผลพลอยได้: บอร์ด Kanban จัดกลุ่มตาม "ผู้รับผิดชอบ" แล้วคลิกชื่อคนในหัวคอลัมน์ (เดิมคลิกไม่มีอะไร
  เกิดขึ้น) จะเปิดหน้าต่างโชว์งานทั้งหมดของคนนั้นแยกเป็นก้อนตามหัวข้อโปรเจค
- ไฟล์หลัก: `apps/web/modules/report_task/store/project-topic-store.ts` (ใหม่, แทน
  `template-store.ts` ที่ลบไป), `components/kanban/new-task-dialog.tsx`,
  `components/kanban/person-topics-dialog.tsx` (ใหม่), `components/kanban/kanban-board.tsx`,
  `components/kanban/kanban-column.tsx`, `lib/db/task-schema.ts` (เพิ่ม `projectTopicId`)
- ต้องทำหลัง pull: ไม่มี migration (ฟิลด์ใหม่อยู่ใน `data` JSON blob ของงาน ไม่ใช่คอลัมน์จริง) —
  แค่ build ใหม่ผ่าน `deploy/release.sh` ตามปกติ
- ค้างอยู่ / ต้องระวัง: **ยังไม่เคยเทสต์จริงบนเซิร์ฟเวอร์เลย** (ไม่มี staging แยก, เขียน/เทสจาก
  เครื่องที่ไม่มี DB ต่อ) — หลัง deploy ควรลองสร้างงานพร้อมหัวข้อใหม่ 1 อัน, เช็คว่าเลือกซ้ำได้ในงาน
  ถัดไป, แล้วลองจัดกลุ่มบอร์ดตามผู้รับผิดชอบ คลิกชื่อคนดูว่า dialog เปิดแยกหัวข้อถูกต้อง ก่อนถือว่าจบงานนี้

### 2026-08-12 14:40 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ลบตัวกรอง "การหักคะแนน" ออกจากแถบตัวกรองบอร์ด Kanban
- ทำอะไร: ผู้ใช้ขอเอา dropdown "การหักคะแนน" (ทั้งหมด/เลยกำหนดทั้งหมด/ยังไม่หัก/หักแล้ว) ออก —
  ปุ่ม "เลยกำหนดเท่านั้น" ที่ทำหน้าที่คล้ายกันยังอยู่เหมือนเดิม
- ไฟล์หลัก: `apps/web/modules/report_task/components/kanban/task-filters.tsx`
- ต้องทำหลัง pull: ไม่มี
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 14:05 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** งานกลุ่มแก้กำหนดส่งได้ทางเดียว (เลือกทีละคน) + บอร์ด Kanban โชว์การ์ด 6 ใบต่อคอลัมน์
- ทำอะไร: (1) งานกลุ่มเดิมมีปุ่ม "แก้ไขกำหนดส่ง" รวมทั้งงานซ้อนกับ "กำหนดส่งแยกรายคน" — สับสนว่า
  แก้อันไหนมีผลกับใคร ตัดปุ่ม/ฟอร์ม/ประวัติกำหนดส่งรวมออกสำหรับงานกลุ่มเท่านั้น (งานเดี่ยวไม่กระทบ)
  (2) ลดจำนวนการ์ดต่อคอลัมน์ Kanban จาก 10 → 6 ใบก่อนต้องกด "แสดงเพิ่มเติม"
- ไฟล์หลัก: `apps/web/modules/report_task/components/kanban/task-detail-sheet.tsx`,
  `components/kanban/kanban-column.tsx`
- ต้องทำหลัง pull: ไม่มี
- ค้างอยู่ / ต้องระวัง: เรื่องคะแนนหักรวมของงานกลุ่มที่ผู้ใช้แจ้งว่าโชว์ไม่ครบ (เช่น -3 แทนที่จะเป็น
  -9 เมื่อ 3 คนเลยกำหนด) — เช็คโค้ดแล้วตัวสูตรรวมคะแนน (`task-card.tsx`) ถูกต้อง คาดว่าเป็นเพราะ
  auto-sweep หักคะแนนทุก 60 วิ (ไม่ใช่ทันที) เลยหักไม่ครบทุกคนตอนแคปภาพ — ยังไม่ยืนยันร้อยเปอร์เซ็นต์
  ว่าไม่มีบั๊กจริง ถ้าเจอซ้ำหลังรอ/รีเฟรชแล้วให้แจ้งกลับมาไล่ต่อ

### 2026-08-12 13:22 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** บันทึกกิจกรรม (`/report-task/activity-log`) แสดงแยกทีละรายการ ไม่ยุบรวมเป็น "×N" แล้ว
- ทำอะไร: ของเดิมยุบเหตุการณ์ที่คนเดียวกันแก้เป้าหมายเดียวกันติดกันใน 5 นาทีให้เหลือแถวเดียว
  พร้อมตัวคูณ (เช่น "เปลี่ยนสถานะ test ×5") ซึ่งบัง timestamp ของแต่ละครั้งไป — ผู้ใช้ขอให้เห็น
  ทุกรายการแยกกันชัด ๆ เลยตัดตรรกะยุบรวมออก
- ไฟล์หลัก: `apps/web/app/(shell)/report-task/activity-log/page.tsx`
- ต้องทำหลัง pull: ไม่มี
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 11:22 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ความยาวรหัสผ่านขั้นต่ำในฟอร์มไม่ตรงกับที่ตรวจจริง ทำหน้า error ตอนสร้าง/รีเซ็ตรหัสผ่าน
- ทำอะไร: หลัง commit `cdd52b7` ของ katawutntp (ค่าเริ่มต้นความยาวรหัสผ่านขยับจาก 8 เป็น 12
  ตัวอักษร ตั้งได้รายบริษัทที่ `/admin/security`) มี 3 ฟอร์มที่ยังเขียน `minLength`/ข้อความ hint
  ตายตัวเป็นเลขเก่า ไม่ตรงกับที่ server ตรวจจริง — กรอกตามที่ฟอร์มบอกว่าพอ แล้วโดนปฏิเสธฝั่ง
  เซิร์ฟเวอร์ กลายเป็นหน้า "This page couldn't load" (server action ไม่มี error boundary จับ)
- ไฟล์หลัก: `apps/web/app/(shell)/account/page.tsx`,
  `apps/web/app/(shell)/admin/users/new/page.tsx`,
  `apps/web/app/(shell)/admin/users/[id]/page.tsx`
- ต้องทำหลัง pull: ไม่มี (ไม่มี migration ใหม่ — แค่ build ใหม่ผ่าน `deploy/release.sh`)
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 10:54 — katawutntp
**feat:** นโยบายล็อกบัญชีและความยาวรหัสผ่านตั้งได้รายบริษัท
- ทำอะไร: เดิม `MAX_FAILED_LOGINS=5`, `LOCK_MINUTES=15`, ความยาวรหัสผ่านขั้นต่ำ เป็นค่าตายตัวในโค้ด
  บังคับทุกบริษัทเหมือนกัน — ย้ายมาตั้งได้ต่อบริษัทที่หน้า `/admin/security` ใหม่ พร้อมปุ่มปลดล็อก
  บัญชีในหน้าผู้ใช้ (มี audit ว่าใครปลดให้ใคร) และข้อความตอนโดนล็อกบอกเวลาที่เหลือ
- ไฟล์/ตารางหลัก: `core.security_settings` (ตารางใหม่), `apps/web/lib/security-settings.ts`,
  `/admin/security`, permission `core.security.setting.manage`
- ต้องทำหลัง pull: **มี migration ใหม่** — รัน `pnpm db:deploy` ก่อน `deploy/release.sh`
- ค้างอยู่ / ต้องระวัง: ค่าเริ่มต้นความยาวรหัสผ่านขยับจาก 8 → 12 ตัวอักษร — ดูรายการถัดขึ้นไป
  (บันทึก 11:22) ที่แก้ฟอร์มที่ยังพังเพราะเรื่องนี้

### 2026-08-12 10:04 — baanpoolvilla (ทำร่วมกับ Claude, Phase 3/3)
**feat:** report_task ดึงแผนก/ตำแหน่งจาก core แทนของในตัวเองแล้ว
- ทำอะไร: จบชุดงาน "แผนก+ตำแหน่งเป็นของกลาง" — โมดูล report_task เลิกเก็บแผนก/ตำแหน่งของตัวเอง
  หันไปอ่านจาก `core.departments`/`core.users.position` (สร้าง/แก้ผ่าน `/admin` แล้ว) แทน
  สร้าง user ที่ `/admin` แล้วแผนก/ตำแหน่งขึ้นในโมดูลนี้ทันที ไม่ต้องตั้งซ้ำ — สี/หัวหน้าแผนก
  ยังเป็นของโมดูลนี้เอง (ไม่ใช่แนวคิดร่วมข้ามโมดูล) เก็บทับไว้แยกต่างหาก
- ไฟล์หลัก: `apps/web/modules/report_task/lib/db/departments.ts` (ใหม่),
  `lib/db/employee-directory.ts`, `store/department-store.ts`,
  `components/shared/org-settings-panel.tsx`, `app/api/report-task/store/[key]/route.ts`
- ต้องทำหลัง pull: ไม่มี (schema มาจาก Phase 1 แล้ว)
- ค้างอยู่ / ต้องระวัง: ยังไม่ได้ทดสอบ end-to-end เต็มรูปแบบบน production (สร้างแผนก →
  assign user → เช็คว่าขึ้นในบอร์ด Kanban/ตั้งค่า report-task ถูกต้อง)

### 2026-08-12 09:46 — baanpoolvilla (ทำร่วมกับ Claude, Phase 2/3)
**feat:** หน้าจัดการแผนก/ตำแหน่ง + สิทธิ์ระดับแผนก/ตำแหน่งข้ามทุกโมดูล
- ทำอะไร: เพิ่มหน้า `/admin/departments`, `/admin/positions` (สร้าง/แก้/ลบ + ตาราง permission
  matrix เหมือน `/admin/roles`) และช่องตั้งแผนก/ตำแหน่งของ user ที่ `/admin/users/[id]` —
  สิทธิ์ที่แผนก/ตำแหน่งกำหนดจะรวมกับสิทธิ์ตาม role ของแต่ละคนตอน login
- ไฟล์หลัก: `apps/web/app/(shell)/admin/departments/*`, `admin/positions/*`,
  `apps/web/modules/admin/data/departments.ts`, `data/positions.ts`,
  `packages/auth/user.ts` (รวมสิทธิ์จาก department/position เข้า JWT)
- ต้องทำหลัง pull: รัน `pnpm db:seed` (idempotent) เพื่อเติม permission
  `core.department.*`/`core.position.*` เข้าแคตตาล็อกที่มีอยู่แล้ว
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 09:33 — baanpoolvilla (ทำร่วมกับ Claude, Phase 1/3)
**feat:** เพิ่ม Department + Position เป็นของกลาง พร้อมสิทธิ์ระดับแผนก/ตำแหน่ง
- ทำอะไร: เพิ่มตาราง `core.departments`, `core.positions`, `core.department_permissions`,
  `core.position_permissions` และคอลัมน์ `users.department_id`/`users.position_id` — จุดเริ่มของ
  ชุดงาน "แผนก+ตำแหน่งไม่ต้องตั้งซ้ำทีละโมดูล" (จบครบที่บันทึก 10:04 ด้านบน)
- ไฟล์หลัก: `packages/database/prisma/schema/core.prisma`,
  migration `20260812100000_add_departments_positions`
- ต้องทำหลัง pull: **มี migration ใหม่** — รัน `pnpm db:deploy`
- ค้างอยู่ / ต้องระวัง: migration เขียนมือ (ไม่มี DB ต่อตอนเขียนให้ `prisma migrate dev` diff ให้)
  แต่ยืนยันแล้วว่า apply ผ่านจริงบน production ไม่มี drift
