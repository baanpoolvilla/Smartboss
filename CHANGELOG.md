# Changelog

**อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้งที่ pull code ใหม่มา** — เพื่อให้รู้ว่าใครแก้อะไรไป
ต้องทำอะไรต่อ (migration/seed/deploy) และมีอะไรที่ยังค้างอยู่บ้าง

## วิธีเพิ่มรายการ

แก้โค้ดเสร็จ (commit แล้ว) ให้เพิ่มบล็อกใหม่ไว้ **บนสุด** ของหัวข้อ "## บันทึก" ด้านล่าง
ตามฟอร์แมตนี้:

```
### YYYY-MM-DD HH:MM — <ชื่อคุณ>
**<feat/fix/docs>:** <สรุปสั้น ๆ ว่าทำอะไร>
- ทำอะไร: <รายละเอียดพอเข้าใจ ไม่ต้องยาว>
- ไฟล์/branch หลัก: <ไฟล์หรือโฟลเดอร์สำคัญ>
- ต้องทำหลัง pull: <migration/seed/deploy ที่ต้องรัน หรือเขียน "ไม่มี">
- ค้างอยู่ / ต้องระวัง: <ถ้ามี>
```

ใช้เวลาปัจจุบันจริง (เขตเวลาไทย) — จะเอามาจาก `git log -1 --date=format:'%Y-%m-%d %H:%M'` ของ
commit ที่เพิ่งทำก็ได้ ไม่ต้องเดา

---

## บันทึก

### 2026-08-13 16:46 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** ปรับหลอดภาระงานรายคน + ตัดคะแนนออกจากหน้านี้ + ลด legend ซ้ำในบอร์ด
- ทำอะไร: หน้า "ภาระงานรายคน" เอาคำว่า "เปิดอยู่" ออกจาก legend, ตัดป้ายคะแนน (เลข/สี/"ยังไม่มีข้อมูล")
  ออกจากตำแหน่งขวาสุดทั้งหมด แทนที่ด้วยจำนวนงานทั้งหมดของคนนั้น ("N งาน") — หน้านี้เป็นเรื่องปริมาณงาน
  ไม่ใช่ผลงาน คะแนน/rating ยังคำนวณและแสดงตามปกติที่หน้า "รายงาน" เหมือนเดิม ไม่กระทบ — ลบโค้ด/import
  คำนวณคะแนนที่ไม่ใช้แล้วออกจากไฟล์นี้ด้วย
- บอร์ด Kanban จัดกลุ่มตามสถานะ: เอา legend ย่อยใต้หลอด (เช่น "● กำลังทำ 1") ออกเฉพาะคอลัมน์สถานะปกติ
  เพราะซ้ำกับหัวคอลัมน์ที่บอกอยู่แล้ว — ยังคงแสดงไว้ตอนจัดกลุ่มตามความสำคัญ/ผู้รับผิดชอบ และคอลัมน์
  "เลยกำหนด" (มีงานหลายสถานะปนกันจริง จำเป็นต้องบอก)
- ไฟล์หลัก: `components/kanban/workload-view.tsx`, `kanban-column.tsx`, `kanban-board.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 16:14 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เพิ่มการตรวจสอบงานเสร็จ (รอเช็ค/ผ่าน) + แถบภาระงานรายคนแสดงงานทั้งหมด
- ทำอะไร: งานที่ "เสร็จสิ้น" ยังไม่มีใครยืนยันว่าตรวจแล้ว จะขึ้นป้าย "รอเช็ค" สีเหลือง ทั้งบนการ์ด Kanban
  และหน้ารายละเอียดงาน — คนมอบหมาย/หัวหน้าแผนก/CEO กดปุ่ม "ผ่าน" ได้ เปลี่ยนเป็น "ตรวจแล้วโดย [ชื่อ]" พร้อม
  แจ้งเตือนคนทำงาน ป้ายรีเซ็ตอัตโนมัติทุกครั้งที่งานหลุดจาก "เสร็จสิ้น" ไม่กระทบสถานะ/คะแนนใดๆ เป็นแค่ชั้น
  ตรวจสอบเสริม — เพิ่ม CEO เข้ารายชื่อที่ได้รับแจ้งเตือนงานเสร็จใกล้/เลยกำหนดด้วย
- หน้า "ภาระงานรายคน": แถบเปลี่ยนจากนับสัดส่วนเฉพาะงานที่ "เปิดอยู่" เป็นนับจากงานทั้งหมด (รอดำเนินการ/
  กำลังทำ/เลยกำหนด/เสร็จ) — "เลยกำหนด" นับเฉพาะงานที่ยังไม่เสร็จและเกินกำหนดจริง ไม่รวมงานที่เสร็จช้าแล้ว —
  ป้ายคะแนนที่เคยขึ้นแดงเสมอตอนยังไม่มีงานเสร็จ (คำนวณได้ 0 เพราะไม่มีข้อมูล ไม่ใช่เพราะทำงานแย่) เปลี่ยน
  เป็น "ยังไม่มีข้อมูล" สีเทาแทน พร้อม hover tooltip รายละเอียดทั้งท่อนสีในแถบและป้ายคะแนน
- ไฟล์หลัก: `types/index.ts`, `store/task-store.ts`, `components/kanban/task-detail-sheet.tsx`,
  `components/kanban/task-card.tsx`, `components/kanban/workload-view.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 15:11 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เลื่อนบอร์ด Kanban ได้สะดวกขึ้น + แก้กำหนดส่งงานกลุ่มทีละคน + ป้องกันปิดงานไม่ครบ checklist
- ทำอะไร: บอร์ด Kanban (จัดกลุ่มตามผู้รับผิดชอบ) และหน้า "งานของ...แยกตามหัวข้อโปรเจค" เพิ่มปุ่มลูกศร
  ซ้าย/ขวา + คลิก-ลากพื้นที่ว่างเพื่อเลื่อน (ไม่ชนกับลากการ์ดย้ายงาน) เพราะคอลัมน์อาจเยอะเกินจอเวลามี
  พนักงาน/โปรเจคหลายคน — หัวคอลัมน์ Kanban เปลี่ยนหลอด "% ของบอร์ด" จากสีเดียวเป็นแบ่งสัดส่วนตามสถานะจริง
  ของงานในคอลัมน์ (รอดำเนินการ/กำลังทำ/เสร็จสิ้น) — หน้า "งานของ...แยกตามหัวข้อโปรเจค" เพิ่มตัวกรองหัวข้อ
  โปรเจค (โผล่เฉพาะมีมากกว่า 1 หัวข้อ) และตัดแถบตัวกรอง/แท็บ/การ์ด KPI ของบอร์ดหลักออกจากหน้านี้ทั้งหมด
  เพราะไม่เกี่ยวกับมุมมองรายคนและไม่มีใครใช้
- "ประวัติการแก้ไขกำหนดส่ง" ของงานกลุ่ม: เปลี่ยนจาก date picker เปิดค้างทุกแถวพร้อมกัน เป็นปุ่ม "แก้ไข
  กำหนดส่ง" กดแล้วค่อยเลือกคน (เฉพาะคนใน task นั้น) ทีละคน — แถวปกติเหลือแค่วันที่อ่านอย่างเดียว ลดความรก
- **ป้องกันปิดงาน "เสร็จสิ้น" ทางลัดถ้า checklist ยังไม่ครบ** — เดิมลากการ์ด/เปลี่ยน dropdown สถานะ/กด
  วงกลม ✓ เร็วๆ/bulk action ในตาราง ทำให้ "เสร็จสิ้น" ได้ทันทีแม้ยังติ๊ก checklist ไม่ครบ ไม่ตรงกับทางที่
  ควรเสร็จอัตโนมัติ (ทุกคนติ๊กครบ) — เพิ่ม `isTaskFullyDone` เป็นเกตกลางใน `moveTask` (บล็อกที่ต้นตอ)
  พร้อม toast บอกว่าเหลือกี่ข้อในทุกจุดที่มีทางลัด
- หน้าตาราง (task-grid): คอลัมน์ "ความคืบหน้า" คงไว้แบบ checklist ratio (ลองเปลี่ยนเป็นนับคนเสร็จแล้ว
  พบว่าหยาบเกินไปสำหรับงานคนเดียว จึงย้อนกลับ — ตอนนี้ปิดงานไม่ครบ checklist ไม่ได้แล้วเลยไม่ขัดกับสถานะ
  อีก) เพิ่ม tooltip อธิบายการเรียงหัวตาราง (คลิกเรียง/ทิศทางลูกศร) และ tooltip รายชื่อผู้รับผิดชอบครบ
  ทุกคน (เดิมเห็นแค่ "+N" กดไม่ได้)
- ไฟล์หลัก: `components/kanban/kanban-board.tsx`, `kanban-column.tsx`, `person-topics-board.tsx`,
  `task-detail-sheet.tsx`, `task-grid.tsx`, `task-card.tsx`, `lib/task-completion.ts`, `store/task-store.ts`,
  `app/(shell)/report-task/tasks/page.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 13:09 — baanpoolvilla
**fix:** จัดระยะขอบหน้า/แถบตัวกรองให้ตรงกันทั้งระบบ + ปรับปุ่มฟิลเตอร์ให้สูงเท่ากัน
- ทำอะไร: แก้ padding ที่ AppScaffold กับแต่ละหน้า (practice, report-feed, settings) เคยประกาศซ้ำกัน
  (pt-4 lg:pt-6 อยู่ทั้งสองที่) รวบให้เหลือจุดเดียวที่ scaffold แทน ปรับ StickyFilterBar ให้ breakpoint
  margin ตรงกับ scaffold เป๊ะทุกจุดพัก ไม่งั้นขอบแถบตัวกรองจะเหลื่อมกับกรอบหน้าตอน resize เพิ่ม
  backdrop-blur ตอน scroll ด้วย และปรับ filterFieldTriggerClass (ปุ่มฟิลเตอร์ทุกหน้าที่ใช้ FilterField
  ร่วมกัน) ให้สูงคงที่ 36px เส้นขอบเต็ม 1px แทน 0.5px (กันเบลอตอน zoom) เพิ่มเงาบางๆ และ focus ring
- ไฟล์หลัก: `components/module/app-scaffold.tsx`, `components/shared/sticky-filter-bar.tsx`,
  `components/shared/filter-field.tsx`, หน้า practice/report-feed/settings
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 13:08 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** จำกัดสิทธิ์เปลี่ยนหัวหน้าหลักของงานกลุ่มเหลือ CEO เท่านั้น + เพิ่มยืนยัน
- ทำอะไร: ผู้ใช้เห็นว่าหัวหน้าหลัก (ดาว ⭐) ของงานกลุ่มเปลี่ยนได้ง่ายเกินไป — เดิมใครก็ตามที่แก้ไข
  งานนี้ได้ (คนสร้างงาน, หัวหน้าแผนกที่งานสังกัด, หรือ CEO) กดดาวเปลี่ยนหัวหน้าได้เลยทันทีไม่มีการยืนยัน
  — จำกัดปุ่มกดดาวเหลือแค่ CEO (isOwner) คนอื่นเห็นดาวเป็นไอคอนอ่านอย่างเดียว เพราะการเปลี่ยนหัวหน้ามีผล
  ต่อ accountability/คะแนนที่ตามหัวหน้าไป และเพิ่ม popup ยืนยันก่อนเปลี่ยนจริง บอกชื่อคนใหม่และเตือนว่า
  หัวหน้าคนเดิม (ถ้ามี) จะพ้นสถานะทันที — กดดาวซ้ำที่คนเดิมจะไม่เด้ง popup มากวนใจเปล่าๆ
- ไฟล์หลัก: `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 13:07 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ป้ายความสำคัญ/สถานะบนการ์ด Kanban ให้สีชัดเจนตามระดับจริง
- ทำอะไร: ผู้ใช้บอกว่าป้ายมุมซ้ายบนของการ์ดงาน (เช่น "ปานกลาง", "สูง") เป็นจุดสีเทาแบนๆ เหมือนกันหมด
  ทุกระดับ มองไม่ออกว่าสำคัญแค่ไหน (โค้ดเดิมตั้งใจทำแบบนี้เพราะกลัวสีซ้ำกับหัวคอลัมน์) — แก้ให้ใช้สีตาม
  ระดับจริงทุกระดับ (ด่วนมาก=แดง, สูง=เหลือง/ส้ม, ปานกลาง=เขียว, ต่ำ=เทา ใช้สีชุดเดียวกับ priorityMeta
  ทั่วระบบ) และทำให้ป้ายนี้สลับอัตโนมัติตามโหมดจัดกลุ่มบอร์ด: จัดกลุ่มตามสถานะ→โชว์ความสำคัญ, จัดกลุ่ม
  ตามความสำคัญ→โชว์สถานะแทน กันไม่ให้ซ้ำกับหัวคอลัมน์ (เดิมจัดกลุ่มตามความสำคัญป้ายก็ยังโชว์ความสำคัญ
  ซ้ำ ไม่มีประโยชน์) — งานเสร็จแล้วยังขึ้นป้าย "เสร็จแล้ว" สีเขียวเหมือนเดิมทุกกรณี ไม่เปลี่ยน
- เพิ่ม accentColor ให้ statusMeta (คู่กับ dot class เดิม) เป็น single source of truth เดียวกับ
  priorityMeta.accentColor
- ไฟล์หลัก: `lib/task-meta.ts`, `components/kanban/task-card.tsx`, `components/kanban/kanban-column.tsx`,
  `components/kanban/kanban-board.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 13:06 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** ปรับปรุงหน้าบันทึกกิจกรรม — ตัวกรองครบ + จัดกลุ่มตามวัน + ไอคอนตามประเภท
- ทำอะไร: ผู้ใช้บอกว่าหน้า "บันทึกกิจกรรม" ยังไม่มีตัวกรองวันที่เหมือนหน้าอื่น และรายการยาวๆ หน้าตา
  เหมือนกันหมดทำให้ตาลาย แยกไม่ออกว่าอันไหนคืออะไร — เพิ่มตัวกรอง "ช่วงเวลา" (ใช้ DateRangeSelectField
  ตัวเดียวกับหน้า Kanban/Dashboard) และ "แผนก" (คำนวณจากแผนกของคนที่ทำ ตรงกับที่หน้านี้จำกัดให้หัวหน้า
  แผนก/ผู้บริหารดูอยู่แล้ว) จัดลำดับตัวกรองกว้าง→แคบ (ช่วงเวลา→แผนก→คนที่ทำ→การกระทำ) แบบเดียวกับตัวกรอง
  หน้าอื่น จัดกลุ่มรายการตามวัน (เส้นคั่น "วันนี้ · ...") และเพิ่มไอคอนสีตามประเภทกิจกรรม (ไฟล์ใหม่
  activity-meta.ts จับคู่ข้อความ action กับไอคอน/สีด้วยคำ ไม่ต้องแก้ทุกครั้งที่มี action ใหม่)
- ไฟล์หลัก: `app/(shell)/report-task/activity-log/page.tsx`, `lib/activity-meta.ts` (ใหม่)
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ระบบบันทึกกิจกรรมตอนนี้ครอบคลุมแค่ 3 โมดูล (งาน, ตั๋วแจ้งปัญหา, หักคะแนน
  อัตโนมัติ) — การประชุม/การลา/โพสต์รายงาน/หน้าตั้งค่าต่างๆ ยังไม่ถูกบันทึกเลย ถ้าอยากให้ครบทุกโมดูล
  ต้องเพิ่ม logActivity ในอีก ~5-6 store เป็นงานถัดไป

### 2026-08-13 13:05 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เพิ่มยืนยันก่อนทำจริงในจุดเสี่ยงกดพลาด + ไอคอน i อธิบายค่าตั้งค่าที่งง
- ทำอะไร: ผู้ใช้กังวลว่ากดปุ่ม "ยกเลิก/เพิ่ม/ตกลง" หลายหน้าในระบบไม่มีการยืนยันอีกรอบ กดพลาดแล้วไม่รู้ตัว
  ว่าไปเปลี่ยน/ลบอะไรไป — ไล่ทั้งโมดูล report_task แล้วเพิ่ม popup ยืนยัน (AlertDialog เดิมของระบบ) 8 จุด:
  สร้างงาน/นัดหมาย/ลา (ยกเลิกตอนกรอกฟอร์มไว้แล้ว), ยกเลิกเชื่อมต่อปฏิทินภายนอก, ลบประเภทการลา, ลบหัวข้อ
  โปรเจกต์, รีเซ็ต dashboard, สร้างอัลบั้ม/งานฝึก/แจ้งปัญหา (ยกเลิกตอนกรอกไว้แล้ว) — ข้ามจุดเล็กในหน้า
  รายละเอียดงาน (แก้ไข inline) เพราะข้อมูลจริงยังไม่ถูกบันทึกอยู่แล้ว ใส่ popup เพิ่มจะรบกวนเปล่าๆ
- เพิ่มไอคอน "i" อธิบาย 3 จุดที่ผู้ใช้บอกว่างงตอนดูครั้งแรก: ตัวเลือกโควตาการลา (ไม่จำกัด/โควตาต่อปี/
  สะสมรายเดือน), วิธีให้วันลาแบบอิงวันหยุดราชการ, หัวข้อ "ตั้งค่าวันหยุดประจำ" (อธิบายว่าต่างจากการลา
  ปกติยังไง) — แพทเทิร์นเดียวกับ commit d8dd0d7
- ไฟล์หลัก: `components/kanban/new-task-dialog.tsx`, `components/calendar/add-calendar-dialog.tsx`,
  `components/calendar/leave-type-settings-dialog.tsx`, `components/calendar/routine-dayoff-settings-dialog.tsx`,
  `components/shared/project-topic-settings-panel.tsx`, `components/dashboard/dashboard-customize-bar.tsx`,
  `components/report-feed/album-form-dialog.tsx`, `components/practice/practice-task-dialog.tsx`,
  `components/issue-report/issue-report-dialog.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 10:34 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** แนบไฟล์ยังพัง (500) — ส่ง error จริงกลับมาเป็น toast + รวมจุดแก้ไข/ดูประวัติกำหนดส่งงานกลุ่ม
- ทำอะไร: ผู้ใช้ทดสอบสร้างงานพร้อมแนบรูป 2 ไฟล์ — ตอนสร้างขึ้นว่าแนบอยู่ (2) แต่พองานสร้างเสร็จเปิด
  ดูกลับเป็น "ไฟล์แนบ (0)" และลองกดแนบใหม่ก็ยัง 500 ซ้ำๆ ทุกครั้ง — เดิมรอ log จาก journalctl ของ
  เพื่อนที่มีสิทธิ์เข้าเซิร์ฟเวอร์แต่ยังไม่ได้มา เลยแก้ให้ route ส่งข้อความ error จริงกลับมาใน response
  เลย (ฝั่ง client อ่าน field นี้มาโชว์เป็น toast อยู่แล้ว) ผู้ใช้จะเห็นสาเหตุจริงทันทีตอนลองครั้งถัดไป
  ไม่ต้องเปิด DevTools หรือขอ log จากใครอีก — ครอบทั้งฟังก์ชันด้วย try/catch ไม่ใช่แค่ putFile เพราะ
  readStore (query ฐานข้อมูล) ก็พังได้เหมือนกันแต่จุดเดิมจับไม่ถึง
- งานกลุ่ม: ย้ายส่วนแก้ไขกำหนดส่งรายคน ("กำหนดส่งแยกรายคน") จากด้านบนมารวมกับ "ประวัติการแก้ไข
  กำหนดส่ง" ที่เดียวกันตามที่ผู้ใช้ขอ (เดิมแก้กับดู log อยู่คนละจุด ต้องเลื่อนไปมา) เพิ่มปุ่ม "แก้ไข
  ทั้งหมด" ตั้งวันเดียวกันให้ทุกคนพร้อมกันได้ในคลิกเดียว (ยังแก้ทีละคนแบบเดิมได้ด้วย)
- ไฟล์หลัก: `app/api/report-task/uploads/route.ts`, `components/kanban/task-detail-sheet.tsx`,
  `store/task-store.ts` (`reviseAllAssigneeDueDates`)
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: **บั๊กอัปโหลด 500 ยังไม่ได้แก้ที่ต้นตอจริงๆ** แค่ทำให้เห็นสาเหตุง่ายขึ้น — รบกวน
  ลองแนบไฟล์อีกครั้งหลัง deploy แล้วส่งข้อความ error (toast สีแดง) ที่ขึ้นมาให้ดู จะรู้สาเหตุจริงชัดๆ

### 2026-08-13 09:59 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เพิ่มไอคอน i อธิบายตัวเลือกช่วงเวลาในตัวกรอง
- ทำอะไร: ผู้ใช้บอกว่าตัวเลือก "ช่วงเวลา" ในตัวกรอง (ทั้งหมด/วันนี้/สะสมรายสัปดาห์/สัปดาห์นี้/สัปดาห์
  ที่แล้ว ฯลฯ) ดูงง โดยเฉพาะกลุ่ม "สะสมราย..." ไม่รู้ว่าวัดจากอะไร ต่างจาก "...นี้"/"...ที่แล้ว" ยังไง
  — เพิ่มไอคอน "i" ข้างป้าย "ช่วงเวลา" กดดูรายละเอียดได้เลย: สะสมราย(สัปดาห์/เดือน/ปี) = นับตั้งแต่
  ต้นรอบถึงวันนี้เท่านั้น (รอบยังไม่จบ), (สัปดาห์/เดือน/ปี)นี้ = ทั้งรอบเต็มๆ รวมวันที่ยังไม่ถึงด้วย,
  (สัปดาห์/เดือน/ปี)ที่แล้ว = รอบก่อนหน้าทั้งรอบ
- เพิ่ม `labelExtra` ให้ `FilterField` ที่ตัวกรองทุกหน้าใช้ร่วมกัน เผื่อฟิลด์อื่นอยากมีคำอธิบายแบบนี้บ้าง
- ไฟล์หลัก: `components/shared/date-range-select-field.tsx`, `components/shared/filter-field.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 09:52 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** งานเสร็จแล้วดูไม่ออกว่าเสร็จ — เพิ่มป้าย "เสร็จแล้ว" + จางการ์ด + แก้ไอคอนถูกจางหาย
- ทำอะไร: ผู้ใช้ทดสอบตัวเลือก D (เอาสีความสำคัญออกจากการ์ด) แล้วบอกว่าการ์ดที่เสร็จแล้วดูยากว่าเสร็จ
  จริงไหม — เช็คโค้ดเจอ 2 จุด: (1) วงกลมติ๊กถูกสีเขียว ไอคอนถูกข้างในตั้งสีเป็น ink (เข้ม) แทนที่จะเป็น
  สีขาว พอวงเล็กๆ มองแทบไม่เห็นเครื่องหมายถูกข้างใน แก้ให้ไอคอนขาวคมชัดบนพื้นเขียว (2) ป้ายความสำคัญ
  มุมซ้ายบนยังโชว์เหมือนงานที่ยังไม่เสร็จทั้งที่ความสำคัญไม่ค่อยมีความหมายแล้วตอนเสร็จ แก้ให้สลับเป็น
  ป้ายเขียว "✓ เสร็จแล้ว" แทนตำแหน่งเดิมเมื่องานเสร็จ — เพิ่มการ์ดทั้งใบจางลงเล็กน้อย (opacity) ด้วย
  ให้โดดออกจากงานที่ยังไม่เสร็จเวลาอยู่คอลัมน์เดียวกัน (เช่นจัดกลุ่มตามความสำคัญ/ผู้รับผิดชอบ ที่งาน
  เสร็จ/ไม่เสร็จปนกันอยู่)
- ไฟล์หลัก: `components/kanban/task-card.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 09:46 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เอาสีความสำคัญออกจากการ์ด Kanban + ปรับลำดับการ์ดใหม่
- ทำอะไร: ผู้ใช้บอกว่าสีของการ์ดดูงงเพราะซ้ำกับสีที่หัวคอลัมน์แสดงอยู่แล้ว (แย่สุดคือ "ปานกลาง"
  กับ "เสร็จสิ้น" ใช้เขียวเหมือนกันเป๊ะ) — ทำหน้าเทียบตัวเลือกสี (A/B/C/D) ให้ดูก่อน ผู้ใช้เลือกตัวเลือก
  D: เอาสีออกจากการ์ดไปเลย เพราะหัวคอลัมน์บอกอยู่แล้วไม่ว่าจะจัดกลุ่มตามอะไร — การ์ดเหลือแค่จุดความ
  สำคัญเล็กๆ เรียบๆ (มีสีจริงเฉพาะ "ด่วนมาก" เท่านั้น) วันครบกำหนดยังเตือนสีแดง/เหลืองไว้เหมือนเดิม
  เพราะเป็นข้อมูลที่หัวคอลัมน์ไม่ได้บอก ไม่ซ้ำกับอะไร
- ปรับลำดับการ์ดตามที่ผู้ใช้อธิบาย: งานเลยกำหนด (ยังไม่เสร็จ) ขึ้นก่อนเป็นกลุ่มแรกเสมอ ตามด้วยงาน
  ที่เหลือ เรียงจากวันที่สร้างก่อนไปหลัง (ของเดิมเรียงตามวันครบกำหนด ไม่ใช่วันสร้าง) งานเสร็จแล้ว
  อยู่ล่างสุดเสมอ
- ไฟล์หลัก: `components/kanban/task-card.tsx`, `lib/task-flags.ts` (`sortTasksForDisplay`)
- ต้องทำหลัง pull: ไม่มี migration ของ report_task — รอบนี้ merge เข้า main พร้อมกับ commit ชุด
  maintenance ของคุณ katawutntp อีกชุด (PM/work-orders/purchase-orders หลายจุด) **ไม่มี migration**
  ในชุดนี้ แต่เพิ่ม dev dependency `tsx` สำหรับรันเทส — `pnpm install` ตามปกติตอน deploy จัดการให้เอง
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 09:07 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** เก็บกวาดจากการตรวจสุขภาพระบบ report_task ทั้งโมดูล
- ทำอะไร: ผู้ใช้ขอให้เช็คภาพรวมว่า report_task ทำงานเป็นไงบ้าง — รัน typecheck/lint (ผ่านหมด 0 error)
  แล้วให้ agent ไล่ตรวจโค้ดที่แก้ไปวันนี้อีกรอบ เจอ 3 จุด: (1) ทูลทิป/คอมเมนต์ในหน้ารายละเอียดงานยัง
  อ้างถึงปุ่ม "เปิดงานใหม่" ที่ลบไปแล้วเมื่อวาน (บอกผู้ใช้ให้กดปุ่มที่ไม่มีอยู่จริงในหน้าจอ) แก้ให้ชี้ไป
  ที่ "แก้ไขกำหนดส่ง" ซึ่งเป็นทางที่ถูกต้องตอนนี้แทน (2) งานกลุ่มที่ถูกลดคนจนเหลือคนเดียว (กลับเป็นงาน
  เดี่ยว) ถ้าคนที่เหลือเคยเป็นหัวหน้าหลักมาก่อน ดาว ⭐ จะยังค้างอยู่ทั้งที่ใช้ไม่ได้แล้วในงานเดี่ยว — แก้
  ให้เคลียร์หัวหน้าหลักทุกครั้งที่กลับเป็นงานเดี่ยว (3) พิมพ์ผิด `assigneeIds[0]!!!!` ใน new-task-dialog.tsx
- ไฟล์หลัก: `components/kanban/task-detail-sheet.tsx`, `store/task-store.ts`,
  `components/kanban/new-task-dialog.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ยังรอคำตอบเรื่องงานเดี่ยว→งานกลุ่มตอน**สร้าง**งานใหม่ (บันทึกไว้ก่อนหน้า) —
  ตรวจแล้วยืนยันว่ายังไม่ได้แก้จริง (หน้าสร้างงานใหม่ยังต้องกดปุ่ม "งานกลุ่ม" เองก่อน)

### 2026-08-13 08:59 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เพิ่มคนคนที่ 2 ในงานเดี่ยวแล้ว ตั้งคนเดิมเป็นหัวหน้าหลักให้อัตโนมัติ
- ทำอะไร: ก่อนหน้านี้พอเพิ่มคนที่ 2 เข้าไปในงานเดี่ยว (สลับเป็นงานกลุ่มอัตโนมัติ) ช่อง "หัวหน้าหลัก"
  ยังว่างอยู่ ต้องมากดดาวเลือกเองทีหลัง — ตอนนี้ถ้ายังไม่มีใครถูกตั้งเป็นหัวหน้ามาก่อน จะตั้งคนที่เป็น
  ผู้รับผิดชอบเดิมของงานเดี่ยวเป็นหัวหน้าหลักให้อัตโนมัติเลย (เขาเป็น "เจ้าของ" งานอยู่แล้วก่อนคนอื่น
  จะเข้ามาร่วม) ถ้ามีคนตั้งหัวหน้าไว้แล้วก่อนหน้านี้จะไม่ไปเขียนทับ — บันทึกลงบันทึกกิจกรรมด้วยว่าเป็น
  การตั้งอัตโนมัติ ไม่ใช่มีคนกดเลือกเอง
- ไฟล์หลัก: `store/task-store.ts` (`setAssignees`)
- ต้องทำหลัง pull: ไม่มี migration ของ report_task — **แต่รอบนี้ merge เข้า main พร้อมกับ commit
  ของคุณ katawutntp** (`feat(maintenance): ตามโครงตาราง changyai...`) ที่มี migration จริงมาด้วย
  (เพิ่ม `work_orders.requires_expense`, `pm_schedules.requires_expense`,
  `work_order_external_photos.note`) ต้องรัน `pnpm db:deploy` ก่อน `release.sh` รอบนี้
- ค้างอยู่ / ต้องระวัง: ยังรอคำตอบเรื่องงานเดี่ยว→งานกลุ่มตอน**สร้าง**งานใหม่ (คนละจุดกับที่แก้รอบนี้
  ซึ่งเป็นตอนแก้ไขงานที่มีอยู่แล้ว) — ดูบันทึกก่อนหน้า

### 2026-08-13 08:56 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** แสดงเลขที่งาน + เดี่ยว/กลุ่ม + หัวข้อโปรเจค แทน uuid ในหัวหน้ารายละเอียดงาน
- ทำอะไร: เดิมหัวข้อบนของหน้ารายละเอียดงานโชว์ id ดิบ (เช่น task-4eca83b3-a867-...) อ่านไม่รู้เรื่อง
  ทั้งที่ระบบมีเลขที่งานให้คนอ่าน (T-2569-0001) ออกให้อัตโนมัติตอนสร้างงานอยู่แล้วในฐานข้อมูล (กันเลข
  ชนกันตอนสองคนสร้างพร้อมกันด้วย) แค่ไม่เคยถูกส่งมาให้ฝั่งเว็บเห็นเลย — เพิ่ม `Task.code` ส่งมาทั้งตอน
  โหลดหน้าและตอนบันทึกงานที่เพิ่งสร้าง (เห็นทันทีไม่ต้องรอโหลดหน้าใหม่) เปลี่ยนหัวข้อเป็นรูปแบบ
  "T-2569-0001 · เดี่ยว/กลุ่ม · <หัวข้อโปรเจค ถ้ามี>" — งานไหนไม่มีหัวข้อโปรเจคก็จะเหลือแค่เลขที่งาน
  กับเดี่ยว/กลุ่ม
- ไฟล์หลัก: `lib/db/task-repo.ts`, `app/api/report-task/tasks/route.ts`,
  `components/shared/task-sync.tsx`, `components/kanban/task-detail-sheet.tsx`, `types/index.ts`
- ต้องทำหลัง pull: ไม่มี migration (คอลัมน์ `code` มีอยู่แล้วในฐานข้อมูล แค่ไม่เคยส่งออกมา)
- ค้างอยู่ / ต้องระวัง: **ยังไม่ได้แก้เรื่องงานเดี่ยว→งานกลุ่ม ตอนสร้างงานใหม่** — ที่คุยค้างไว้ว่า
  หน้าสร้างงานใหม่กับหน้ารายละเอียดงานพฤติกรรมไม่ตรงกัน (หน้าสร้างงานใหม่ต้องกดปุ่ม "งานกลุ่ม" เอง
  ก่อนถึงจะเพิ่มคนที่ 2 ได้ ไม่สลับให้อัตโนมัติเหมือนหน้ารายละเอียด) รอคำตอบว่าจะให้แก้ไหม

### 2026-08-13 08:44 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** แนบไฟล์/รูปได้ตอนสร้างงานใหม่ (เดิมมีแต่ตอนสร้างประชุม)
- ทำอะไร: หน้า "สร้างรายการใหม่" แท็บ "ประชุม" มีช่องแนบไฟล์อยู่แล้ว แต่แท็บ "งาน" ไม่มี — เพิ่มช่อง
  "ไฟล์แนบ / รูปภาพ" ให้แท็บงานด้วย ตำแหน่งหลังเช็คลิสต์ อัปโหลดจริงผ่าน /api/report-task/uploads
  เดียวกับหน้ารายละเอียดงาน (ทางเดียวกับที่ใช้แก้บั๊ก 500 เมื่อครู่) — ระหว่างทางเจอว่าไฟล์แนบตอน
  สร้างประชุมเองก็ยังไม่ใช่ของจริงสำหรับไฟล์ที่ไม่ใช่รูปภาพ (แค่เก็บชื่อ/ขนาดไว้เฉยๆ ไม่มี url จริง
  เหมือนบั๊กเก่าที่เคยแก้ในหน้ารายละเอียดงานไปแล้ว) แก้ให้เป็นของจริงพร้อมกันทั้งสองที่เลย — ไฟล์ไหน
  อัปโหลดไม่ผ่าน (ชนิด/ขนาดเกินที่ตั้งค่าไว้) จะแจ้งเตือนแยกเฉพาะไฟล์นั้น ไม่ทำให้สร้างงาน/ประชุม
  ทั้งหมดล้มเหลวไปด้วย
- ไฟล์หลัก: `components/kanban/new-task-dialog.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-13 08:39 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** แนบไฟล์ยังพังบ้าง (400/500) — จับ error ตอนเซฟไฟล์ + log ไว้ดู
- ทำอะไร: ผู้ใช้ทดสอบล่าสุดยังเจอทั้ง POST /api/report-task/uploads ขึ้น 400 (บางไฟล์) และ 500
  (บางไฟล์) แต่ผู้ใช้เช็ค DevTools ต่อไม่ไหว (ไม่ใช่สายเทคนิค) เลยระบุสาเหตุแน่ชัดจากฝั่งนี้ไม่ได้
  ในตอนนี้ — ปรับ route ให้ช่วยดีบักได้เอง: ข้อความ error ตอน 400 (ชนิดไฟล์ไม่รองรับ) บอกชนิดไฟล์จริง
  ที่ส่งมาด้วย, ครอบ `putFile` (จุดเดียวที่พังได้จากเหตุนอกเหนือคำขอเอง เช่น S3/ดิสก์เขียนไม่ได้) ด้วย
  try/catch แล้ว log รายละเอียด error จริงด้วย `console.error` ฝั่งเซิร์ฟเวอร์ แทนที่จะปล่อยให้เด้ง 500
  เปล่าๆ ไม่มีร่องรอย
- ไฟล์หลัก: `app/api/report-task/uploads/route.ts`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: **ยังไม่ได้แก้บั๊กต้นตอ** — ถ้ายัง 500 อยู่หลัง deploy รอบนี้ ให้เพื่อนรัน
  `sudo journalctl -u smartboss-web -n 100 --no-pager | grep uploads` หลังลองแนบไฟล์อีกครั้ง แล้ว
  ส่งบรรทัด `[uploads] putFile failed ...` มาให้ดู จะรู้สาเหตุจริงชัดๆ (เดาไว้ว่าอาจเป็นเรื่อง
  S3/MinIO ยังไม่ได้ตั้งค่าให้ครบใน `/etc/smartboss/smartboss.env` หรือโฟลเดอร์ local disk เขียนไม่ได้)

### 2026-08-13 08:25 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** เอาลุคปุ่มออกจาก "มอบหมายโดย" ที่แก้ไม่ได้แล้ว
- ทำอะไร: ก่อนหน้านี้ล็อกไม่ให้เปลี่ยน "มอบหมายโดย" ไปแล้ว (ดูบันทึกวันที่ 12 ส.ค. ช่วง 16:02) แต่
  ยังใช้สไตล์เส้นขอบ/พื้นหลังแบบปุ่มเดิมอยู่ ผู้ใช้ทดสอบแล้วเข้าใจผิดว่ายังกดเปลี่ยนได้ (กดแล้วไม่มี
  อะไรเกิดขึ้น) — เปลี่ยนเป็นข้อความเปล่าไม่มีกรอบ ให้ดูออกชัดว่าเป็นข้อมูลอ่านอย่างเดียว
- ไฟล์หลัก: `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 16:50 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เพิ่มหน้าตั้งค่าจัดการหัวข้อโปรเจค (เปลี่ยนชื่อ/สี/ลบ)
- ทำอะไร: เดิม "หัวข้อโปรเจค" สร้างได้อย่างเดียวจากปุ่ม "+ สร้างหัวข้อใหม่" ตอนสร้างงาน ไม่มีที่แก้ไข/
  ลบเลย เพิ่มแท็บย่อย "หัวข้อโปรเจค" ในหน้าตั้งค่า > งาน ให้เปลี่ยนชื่อ/ตั้งสี/ลบได้จากที่เดียว — ปุ่ม
  สร้างเร็วๆ ตอนสร้างงานยังใช้ได้เหมือนเดิม สองทางนี้ไม่ชนกัน ลบหัวข้อไหนในหน้าตั้งค่า งานที่เคยติด
  หัวข้อนั้นจะกลายเป็น "ไม่ระบุหัวข้อโปรเจค" แทนที่จะหายไปเงียบๆ จากหน้ารายการงานแยกหัวข้อของแต่ละคน
  (เช็คโค้ดเจอว่าเดิมจะเป็นแบบนั้นถ้าลบตรงๆ)
- เพิ่ม "projectTopics" เป็นสิทธิ์ที่มอบหมายให้พนักงานคนอื่นจัดการแทนเจ้าของบริษัทได้ เหมือนหัวข้ออื่น
  ในหน้าตั้งค่า (สติกเกอร์, ไฟล์แนบ ฯลฯ)
- ไฟล์หลัก: `components/shared/project-topic-settings-panel.tsx` (ใหม่), `store/project-topic-store.ts`,
  `store/settings-access-store.ts`, `app/(shell)/report-task/settings/page.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 16:39 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** บันทึกกิจกรรมยังไม่ครบ — เพิ่ม log การแก้ไขชื่อ/รายละเอียด/ความสำคัญ/วันเริ่มต้น/หัวหน้าหลัก
- ทำอะไร: ผู้ใช้ตรวจหน้า "บันทึกกิจกรรม" แล้วเจอว่าการแก้ไขหลายอย่างไม่ถูกบันทึกเลย — สาเหตุคือของเดิม
  ตั้งใจไม่ log การเรียก `updateTask` ตรงๆ เพราะกลัวช่องพิมพ์ข้อความ (เช่นแก้ชื่องาน) ยิง log รัวทุก
  ตัวอักษร แต่บางจุดที่ใช้ updateTask ตรงๆ จริงๆ แล้วเป็นการกดยืนยันครั้งเดียว (ปุ่มบันทึก/เลือกจาก
  dropdown) ไม่ใช่การพิมพ์ — log ได้โดยไม่ท่วม จึงเพิ่ม action ใหม่ที่ log ด้วย: แก้ไขชื่อ/รายละเอียด
  งาน (บันทึกครั้งเดียวตอนกดปุ่ม ไม่ใช่ทุกตัวอักษร), เปลี่ยนความสำคัญ, เปลี่ยนวันเริ่มต้น — และ "ตั้ง
  หัวหน้าหลัก" (ฟีเจอร์ที่เพิ่งเพิ่มไปก่อนหน้านี้ในรอบเดียวกัน) ที่ลืม log ไปตอนแรกด้วย
- ไฟล์หลัก: `store/task-store.ts` (`saveTaskDetails`, `setPriority`, `setStartDate`,
  `setMainAssignee`), `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: เช็คลิสต์/คอมเมนต์/ไฟล์แนบยังตั้งใจไม่ log อยู่ (ความถี่สูง ความเสี่ยงต่ำ) —
  ถ้าอยากให้ log ด้วยบอกได้เลย

### 2026-08-12 16:32 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** เรียงการ์ดงานอัตโนมัติ (เสร็จลงล่าง/เรียงตามความเร่งด่วน) + เลือก "หัวหน้าหลัก" ของงานกลุ่มได้
- ทำอะไร:
  1. การ์ดงานในทุกคอลัมน์ของบอร์ด Kanban และหน้ารายการงานแยกหัวข้อโปรเจคของแต่ละคน จะเรียงลำดับ
     อัตโนมัติแล้ว — งาน "เสร็จสิ้น" ลงไปอยู่ล่างสุดเสมอ ส่วนงานที่ยังไม่เสร็จเรียงจากกำหนดส่งใกล้สุด/
     เลยกำหนดมากสุดขึ้นก่อน
  2. งานที่มีผู้รับผิดชอบหลายคน (งานกลุ่ม) เลือกได้ว่าใครเป็น "หัวหน้าหลัก" — เลือกตอนสร้างงานได้เลย
     หรือกดดาว ⭐ ข้างชื่อทีหลังในหน้ารายละเอียดงานก็ได้ เป็นแค่ป้ายแสดงผล **ไม่มีผลกับสิทธิ์แก้ไข/
     มองเห็นงาน** (สิทธิ์ยังมาจากผู้มอบหมาย/หัวหน้าแผนกเหมือนเดิม) ถอดคนที่เป็นหัวหน้าออกจากผู้
     รับผิดชอบ ป้ายจะหลุดให้เองอัตโนมัติ
- ไฟล์หลัก: `lib/task-flags.ts` (`sortTasksForDisplay`), `components/kanban/kanban-board.tsx`,
  `components/kanban/person-topics-board.tsx`, `store/task-store.ts` (`setMainAssignee`),
  `components/kanban/new-task-dialog.tsx`, `components/kanban/task-detail-sheet.tsx`,
  `components/kanban/task-card.tsx`, `types/index.ts` (`Task.mainAssigneeId`)
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 16:17 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** แก้อัปโหลดไฟล์แนบ/รูปไม่ได้เลย (POST /api/report-task/uploads ขึ้น 500)
- ทำอะไร: ผู้ใช้ทดสอบจริงบนโปรดักชันแล้วกด "แนบไฟล์" ไม่มีอะไรเกิดขึ้นเลย เช็ค DevTools เจอ
  `POST /api/report-task/uploads 500` — สาเหตุคือโค้ดเรียก `crypto.randomUUID()` แบบพึ่ง global
  ของ Node ตรงๆ บนเซิร์ฟเวอร์ ซึ่งไม่การันตีว่ามีเสมอทุกเวอร์ชัน Node ต่างจาก `storage.ts` (ใช้งาน
  ได้จริงอยู่แล้ว) ที่ import `randomUUID` จาก `"node:crypto"` ตรงๆ — แก้ให้ทุกจุดที่เรียกบนเซิร์ฟเวอร์
  ใช้ import แบบเดียวกันหมด แก้ไปด้วยว่า `/api/report-task/tasks/sweep` (หักคะแนนงานเลยกำหนด
  อัตโนมัติ) ก็เรียกแบบเดียวกัน แต่ error ถูกกลืนเงียบๆ ไม่มี toast แจ้ง จึงพังมาเงียบๆ โดยไม่มีใครรู้
- เปลี่ยนปุ่ม "+ ตกลง" ในช่องเพิ่มเช็คลิสต์ (หน้ารายละเอียดงาน) เป็น "+ เพิ่ม" ให้ตรงกับปุ่มเดียวกัน
  ในหน้าสร้างงานใหม่
- ไฟล์หลัก: `app/api/report-task/uploads/route.ts`, `app/api/report-task/tasks/sweep/route.ts`,
  `lib/db/ics-link-repo.ts`, `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ถ้า deploy แล้วยังอัปโหลดไม่ได้ ให้เช็คว่า `sudo -u smartboss node --version`
  บนเครื่อง production ตรงกับที่ package.json กำหนดไว้ (`>=20`) จริงไหม — ถ้าต่ำกว่านี้อาจมีจุดพังแบบ
  เดียวกันซ่อนอยู่ที่อื่นอีก

### 2026-08-12 16:02 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat/fix:** สลับโหมดงานเดี่ยว/กลุ่มอัตโนมัติตามจำนวนผู้รับผิดชอบ + ล็อก "มอบหมายโดย" ห้ามเปลี่ยน
- ทำอะไร: เดิมกด "+ เพิ่มคน" ในผู้รับผิดชอบได้เสมอไม่ว่างานจะเป็นเดี่ยวหรือกลุ่ม แต่ `taskMode` ไม่เคย
  อัปเดตตาม ทำให้งานที่เพิ่มเป็น 2 คนแต่ยังนับเป็น "เดี่ยว" กดสถานะเป็น "เสร็จสิ้น" ตรงๆ ได้เลยโดยไม่รอ
  ให้ทุกคนติ๊กเช็คลิสต์ครบก่อน (ขัดกับพฤติกรรมงานกลุ่มจริง) — แก้โดยให้ `setAssignees` (จุดเดียวที่ทุก
  หน้าจอเรียกใช้ตอนเพิ่ม/ลดคน) สลับ `taskMode` ให้เองอัตโนมัติ: เหลือ 1 คน = งานเดี่ยว, 2 คนขึ้นไป =
  งานกลุ่ม ไม่ต้องมีเมนูแยกให้เลือกโหมด และล็อกช่อง "มอบหมายโดย" ในหน้ารายละเอียดงานให้อ่านอย่างเดียว
  เสมอ (เดิมผู้จัดการเปลี่ยนได้) เพราะเป็นข้อเท็จจริงว่าใครมอบหมายงานจริง ไม่ควรแก้ย้อนหลังได้ — มีผลกับ
  สิทธิ์แก้ไข/มองเห็นงานด้วย
- เพิ่มไอคอน "i" ข้างหัวข้อ "ไฟล์แนบ" ในหน้ารายละเอียดงาน (ทั้งไฟล์แนบของงานและไฟล์แนบในคอมเมนต์)
  กดดูได้ว่าบริษัทนี้ตั้งขนาด/จำนวนไฟล์สูงสุดไว้เท่าไหร่ (อ่านค่าจากหน้าตั้งค่า "ไฟล์แนบ" เดียวกัน)
- ไฟล์หลัก: `store/task-store.ts` (`setAssignees`), `components/kanban/task-detail-sheet.tsx`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 15:32 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ลบฟีเจอร์ "เปิดงานใหม่" ให้แก้ไขกำหนดส่งดึงงานที่เสร็จกลับมาทำแทน
- ทำอะไร: เอาปุ่ม/ฟอร์ม "เปิดงานใหม่" กับป้าย "แก้ไขงาน — งานยังไม่เรียบร้อย (N ครั้ง)" ออกทั้งจาก
  การ์ด Kanban และแผงรายละเอียดงาน (สาเหตุที่ตัวนับมักขึ้น 0 ทั้งที่มีป้ายแสดง: `moveTask` ตั้ง
  `reopenedOnce` เองทุกครั้งที่ลากงานออกจาก "เสร็จสิ้น" คนละทางกับตัวนับจริงที่มาจาก revision
  ที่ติดแท็กตอนกดเปิดงานใหม่เท่านั้น — เลยไม่ตรงกัน) แทนที่ด้วย: ถ้าใช้ "แก้ไขกำหนดส่ง" กับงานที่
  สถานะ "เสร็จสิ้น" อยู่ ระบบจะดึงสถานะกลับเป็น "กำลังทำ" ให้อัตโนมัติในตัวเลย ไม่ต้องมีปุ่มแยก
- ไฟล์หลัก: `store/task-store.ts` (ลบ action `reopenTask`, แก้ `reviseDueDate`),
  `components/kanban/task-card.tsx`, `components/kanban/task-detail-sheet.tsx`,
  `lib/task-flags.ts` (ลบ `reopenCount` ที่ไม่มีที่ใช้แล้ว)
- ต้องทำหลัง pull: ไม่มี migration (field `reopenedOnce` เดิมใน type/schema ปล่อยไว้เฉยๆ
  เผื่องานเก่าที่มีค่านี้อยู่ ไม่กระทบอะไร)
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 (ช่วงบ่าย) — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** แนบไฟล์จริงในคอมเมนต์ + ตั้งค่าขนาด/จำนวนไฟล์แนบได้เองต่อบริษัท
- ทำอะไร: ช่องคอมเมนต์เอาปุ่มแท็ก @ ออก เปลี่ยนเป็นแนบไฟล์/รูปแทน (อัปโหลดจริง ไม่ใช่ metadata
  ปลอมเหมือนของเดิม) — ระหว่างทางเจอว่าปุ่ม "แนบไฟล์" ในหน้ารายละเอียดงานเป็นของปลอมมาตลอด
  (สร้างไฟล์ "1.2 MB PDF" หลอกๆ ไม่อัปโหลดจริง) แก้ให้เป็นของจริงพร้อมกัน — เพิ่มหน้าตั้งค่า
  "ไฟล์แนบ" ให้ปรับขนาดสูงสุดต่อไฟล์ (แยกรูป/เอกสาร/วิดีโอ) และจำนวนไฟล์สูงสุดต่องาน/ต่อ
  ความคิดเห็นได้เอง — ฝั่งเซิร์ฟเวอร์ (`/api/report-task/uploads`) บังคับตามค่านี้จริง ไม่ใช่แค่ UI
- ไฟล์หลัก: `apps/web/modules/report_task/store/attachment-settings-store.ts` (ใหม่),
  `lib/task-attachment-upload.ts` (ใหม่), `components/shared/attachment-settings-panel.tsx` (ใหม่),
  `components/kanban/task-detail-sheet.tsx`, `app/api/report-task/uploads/route.ts`
- ต้องทำหลัง pull: ไม่มี migration
- ค้างอยู่ / ต้องระวัง: **ยังไม่ผูกกับแพ็กเกจสมัครสมาชิก (planCode) อัตโนมัติ** — ตอนนี้เป็นค่าที่
  เจ้าของบริษัทตั้งเอง (ทุกบริษัทเริ่มด้วยค่าเริ่มต้นเดียวกัน: รูป/เอกสาร 8MB, วิดีโอ 25MB, งานละ 20
  ไฟล์, คอมเมนต์ละ 5 ไฟล์) ยังไม่มีจุดไหนในระบบอ่าน `planCode` ไปจำกัดอะไรอัตโนมัติ — รอตัวเลขจริง
  ต่อแพ็กเกจถึงจะผูกอัตโนมัติได้ ก็ยังไม่ได้แก้ไฟล์ที่ไม่ใช่รูปในตอนสร้างงาน/ประชุม (`new-task-dialog.tsx`)
  ให้อัปโหลดจริง — ยังเป็นข้อจำกัดเดิมที่ค้างมาก่อนรอบนี้

### 2026-08-12 15:30 — baanpoolvilla (แก้ร่วมกับ Claude)
**feat:** "หัวข้อโปรเจค" — แท็กจัดกลุ่มงานใหม่ แทนระบบเทมเพลตเดิม (ยังไม่ deploy — รอทดสอบ)
- ทำอะไร: เพิ่มแท็ก "หัวข้อโปรเจค" ให้ติดกับงานได้ (ไม่บังคับ ไม่มีผลสิทธิ์อะไร) แทนที่ช่อง
  "เทมเพลต" เดิมในฟอร์มสร้างงาน — เลือกหัวข้อที่มีอยู่ หรือ "+ สร้างหัวข้อใหม่..." ได้เลย
  ผลพลอยได้: บอร์ด Kanban จัดกลุ่มตาม "ผู้รับผิดชอบ" แล้วคลิกชื่อคนในหัวคอลัมน์ (เดิมคลิกไม่มีอะไร
  เกิดขึ้น) จะเปิดหน้าต่างโชว์งานทั้งหมดของคนนั้นแยกเป็นก้อนตามหัวข้อโปรเจค
- ไฟล์หลัก: `apps/web/modules/report_task/store/project-topic-store.ts` (ใหม่, แทน
  `template-store.ts` ที่ลบไป), `components/kanban/new-task-dialog.tsx`,
  `components/kanban/person-topics-dialog.tsx` (ใหม่), `components/kanban/kanban-board.tsx`,
  `components/kanban/kanban-column.tsx`, `lib/db/task-schema.ts` (เพิ่ม `projectTopicId`)
- ต้องทำหลัง pull: ไม่มี migration (ฟิลด์ใหม่อยู่ใน `data` JSON blob ของงาน ไม่ใช่คอลัมน์จริง) —
  แค่ build ใหม่ผ่าน `deploy/release.sh` ตามปกติ
- ค้างอยู่ / ต้องระวัง: **ยังไม่เคยเทสต์จริงบนเซิร์ฟเวอร์เลย** (ไม่มี staging แยก, เขียน/เทสจาก
  เครื่องที่ไม่มี DB ต่อ) — หลัง deploy ควรลองสร้างงานพร้อมหัวข้อใหม่ 1 อัน, เช็คว่าเลือกซ้ำได้ในงาน
  ถัดไป, แล้วลองจัดกลุ่มบอร์ดตามผู้รับผิดชอบ คลิกชื่อคนดูว่า dialog เปิดแยกหัวข้อถูกต้อง ก่อนถือว่าจบงานนี้

### 2026-08-12 14:40 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ลบตัวกรอง "การหักคะแนน" ออกจากแถบตัวกรองบอร์ด Kanban
- ทำอะไร: ผู้ใช้ขอเอา dropdown "การหักคะแนน" (ทั้งหมด/เลยกำหนดทั้งหมด/ยังไม่หัก/หักแล้ว) ออก —
  ปุ่ม "เลยกำหนดเท่านั้น" ที่ทำหน้าที่คล้ายกันยังอยู่เหมือนเดิม
- ไฟล์หลัก: `apps/web/modules/report_task/components/kanban/task-filters.tsx`
- ต้องทำหลัง pull: ไม่มี
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 14:05 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** งานกลุ่มแก้กำหนดส่งได้ทางเดียว (เลือกทีละคน) + บอร์ด Kanban โชว์การ์ด 6 ใบต่อคอลัมน์
- ทำอะไร: (1) งานกลุ่มเดิมมีปุ่ม "แก้ไขกำหนดส่ง" รวมทั้งงานซ้อนกับ "กำหนดส่งแยกรายคน" — สับสนว่า
  แก้อันไหนมีผลกับใคร ตัดปุ่ม/ฟอร์ม/ประวัติกำหนดส่งรวมออกสำหรับงานกลุ่มเท่านั้น (งานเดี่ยวไม่กระทบ)
  (2) ลดจำนวนการ์ดต่อคอลัมน์ Kanban จาก 10 → 6 ใบก่อนต้องกด "แสดงเพิ่มเติม"
- ไฟล์หลัก: `apps/web/modules/report_task/components/kanban/task-detail-sheet.tsx`,
  `components/kanban/kanban-column.tsx`
- ต้องทำหลัง pull: ไม่มี
- ค้างอยู่ / ต้องระวัง: เรื่องคะแนนหักรวมของงานกลุ่มที่ผู้ใช้แจ้งว่าโชว์ไม่ครบ (เช่น -3 แทนที่จะเป็น
  -9 เมื่อ 3 คนเลยกำหนด) — เช็คโค้ดแล้วตัวสูตรรวมคะแนน (`task-card.tsx`) ถูกต้อง คาดว่าเป็นเพราะ
  auto-sweep หักคะแนนทุก 60 วิ (ไม่ใช่ทันที) เลยหักไม่ครบทุกคนตอนแคปภาพ — ยังไม่ยืนยันร้อยเปอร์เซ็นต์
  ว่าไม่มีบั๊กจริง ถ้าเจอซ้ำหลังรอ/รีเฟรชแล้วให้แจ้งกลับมาไล่ต่อ

### 2026-08-12 13:22 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** บันทึกกิจกรรม (`/report-task/activity-log`) แสดงแยกทีละรายการ ไม่ยุบรวมเป็น "×N" แล้ว
- ทำอะไร: ของเดิมยุบเหตุการณ์ที่คนเดียวกันแก้เป้าหมายเดียวกันติดกันใน 5 นาทีให้เหลือแถวเดียว
  พร้อมตัวคูณ (เช่น "เปลี่ยนสถานะ test ×5") ซึ่งบัง timestamp ของแต่ละครั้งไป — ผู้ใช้ขอให้เห็น
  ทุกรายการแยกกันชัด ๆ เลยตัดตรรกะยุบรวมออก
- ไฟล์หลัก: `apps/web/app/(shell)/report-task/activity-log/page.tsx`
- ต้องทำหลัง pull: ไม่มี
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 11:22 — baanpoolvilla (แก้ร่วมกับ Claude)
**fix:** ความยาวรหัสผ่านขั้นต่ำในฟอร์มไม่ตรงกับที่ตรวจจริง ทำหน้า error ตอนสร้าง/รีเซ็ตรหัสผ่าน
- ทำอะไร: หลัง commit `cdd52b7` ของ katawutntp (ค่าเริ่มต้นความยาวรหัสผ่านขยับจาก 8 เป็น 12
  ตัวอักษร ตั้งได้รายบริษัทที่ `/admin/security`) มี 3 ฟอร์มที่ยังเขียน `minLength`/ข้อความ hint
  ตายตัวเป็นเลขเก่า ไม่ตรงกับที่ server ตรวจจริง — กรอกตามที่ฟอร์มบอกว่าพอ แล้วโดนปฏิเสธฝั่ง
  เซิร์ฟเวอร์ กลายเป็นหน้า "This page couldn't load" (server action ไม่มี error boundary จับ)
- ไฟล์หลัก: `apps/web/app/(shell)/account/page.tsx`,
  `apps/web/app/(shell)/admin/users/new/page.tsx`,
  `apps/web/app/(shell)/admin/users/[id]/page.tsx`
- ต้องทำหลัง pull: ไม่มี (ไม่มี migration ใหม่ — แค่ build ใหม่ผ่าน `deploy/release.sh`)
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 10:54 — katawutntp
**feat:** นโยบายล็อกบัญชีและความยาวรหัสผ่านตั้งได้รายบริษัท
- ทำอะไร: เดิม `MAX_FAILED_LOGINS=5`, `LOCK_MINUTES=15`, ความยาวรหัสผ่านขั้นต่ำ เป็นค่าตายตัวในโค้ด
  บังคับทุกบริษัทเหมือนกัน — ย้ายมาตั้งได้ต่อบริษัทที่หน้า `/admin/security` ใหม่ พร้อมปุ่มปลดล็อก
  บัญชีในหน้าผู้ใช้ (มี audit ว่าใครปลดให้ใคร) และข้อความตอนโดนล็อกบอกเวลาที่เหลือ
- ไฟล์/ตารางหลัก: `core.security_settings` (ตารางใหม่), `apps/web/lib/security-settings.ts`,
  `/admin/security`, permission `core.security.setting.manage`
- ต้องทำหลัง pull: **มี migration ใหม่** — รัน `pnpm db:deploy` ก่อน `deploy/release.sh`
- ค้างอยู่ / ต้องระวัง: ค่าเริ่มต้นความยาวรหัสผ่านขยับจาก 8 → 12 ตัวอักษร — ดูรายการถัดขึ้นไป
  (บันทึก 11:22) ที่แก้ฟอร์มที่ยังพังเพราะเรื่องนี้

### 2026-08-12 10:04 — baanpoolvilla (ทำร่วมกับ Claude, Phase 3/3)
**feat:** report_task ดึงแผนก/ตำแหน่งจาก core แทนของในตัวเองแล้ว
- ทำอะไร: จบชุดงาน "แผนก+ตำแหน่งเป็นของกลาง" — โมดูล report_task เลิกเก็บแผนก/ตำแหน่งของตัวเอง
  หันไปอ่านจาก `core.departments`/`core.users.position` (สร้าง/แก้ผ่าน `/admin` แล้ว) แทน
  สร้าง user ที่ `/admin` แล้วแผนก/ตำแหน่งขึ้นในโมดูลนี้ทันที ไม่ต้องตั้งซ้ำ — สี/หัวหน้าแผนก
  ยังเป็นของโมดูลนี้เอง (ไม่ใช่แนวคิดร่วมข้ามโมดูล) เก็บทับไว้แยกต่างหาก
- ไฟล์หลัก: `apps/web/modules/report_task/lib/db/departments.ts` (ใหม่),
  `lib/db/employee-directory.ts`, `store/department-store.ts`,
  `components/shared/org-settings-panel.tsx`, `app/api/report-task/store/[key]/route.ts`
- ต้องทำหลัง pull: ไม่มี (schema มาจาก Phase 1 แล้ว)
- ค้างอยู่ / ต้องระวัง: ยังไม่ได้ทดสอบ end-to-end เต็มรูปแบบบน production (สร้างแผนก →
  assign user → เช็คว่าขึ้นในบอร์ด Kanban/ตั้งค่า report-task ถูกต้อง)

### 2026-08-12 09:46 — baanpoolvilla (ทำร่วมกับ Claude, Phase 2/3)
**feat:** หน้าจัดการแผนก/ตำแหน่ง + สิทธิ์ระดับแผนก/ตำแหน่งข้ามทุกโมดูล
- ทำอะไร: เพิ่มหน้า `/admin/departments`, `/admin/positions` (สร้าง/แก้/ลบ + ตาราง permission
  matrix เหมือน `/admin/roles`) และช่องตั้งแผนก/ตำแหน่งของ user ที่ `/admin/users/[id]` —
  สิทธิ์ที่แผนก/ตำแหน่งกำหนดจะรวมกับสิทธิ์ตาม role ของแต่ละคนตอน login
- ไฟล์หลัก: `apps/web/app/(shell)/admin/departments/*`, `admin/positions/*`,
  `apps/web/modules/admin/data/departments.ts`, `data/positions.ts`,
  `packages/auth/user.ts` (รวมสิทธิ์จาก department/position เข้า JWT)
- ต้องทำหลัง pull: รัน `pnpm db:seed` (idempotent) เพื่อเติม permission
  `core.department.*`/`core.position.*` เข้าแคตตาล็อกที่มีอยู่แล้ว
- ค้างอยู่ / ต้องระวัง: ไม่มี

### 2026-08-12 09:33 — baanpoolvilla (ทำร่วมกับ Claude, Phase 1/3)
**feat:** เพิ่ม Department + Position เป็นของกลาง พร้อมสิทธิ์ระดับแผนก/ตำแหน่ง
- ทำอะไร: เพิ่มตาราง `core.departments`, `core.positions`, `core.department_permissions`,
  `core.position_permissions` และคอลัมน์ `users.department_id`/`users.position_id` — จุดเริ่มของ
  ชุดงาน "แผนก+ตำแหน่งไม่ต้องตั้งซ้ำทีละโมดูล" (จบครบที่บันทึก 10:04 ด้านบน)
- ไฟล์หลัก: `packages/database/prisma/schema/core.prisma`,
  migration `20260812100000_add_departments_positions`
- ต้องทำหลัง pull: **มี migration ใหม่** — รัน `pnpm db:deploy`
- ค้างอยู่ / ต้องระวัง: migration เขียนมือ (ไม่มี DB ต่อตอนเขียนให้ `prisma migrate dev` diff ให้)
  แต่ยืนยันแล้วว่า apply ผ่านจริงบน production ไม่มี drift
